# GitHub API Token (optionnel)
# Pour éviter les limites de l'API GitHub (60 requêtes/heure sans auth)
# Avec un token : 5000 requêtes/heure
# Créez un token sur : https://github.com/settings/tokens
# Permissions requises : public_repo (lecture seule)
VITE_GITHUB_TOKEN=
